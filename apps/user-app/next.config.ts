import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Temporarily disable Turbopack if there are CSS processing issues
  // experimental: {
  //   turbo: {
  //     // Turbopack options
  //   },
  // },
};

export default nextConfig;




